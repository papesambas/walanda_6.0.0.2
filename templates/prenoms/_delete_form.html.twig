<form method="post" action="{{ path('app_prenoms_delete', {'id': prenom.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prenom.id) }}">
    <button class="btn">Delete</button>
</form>
