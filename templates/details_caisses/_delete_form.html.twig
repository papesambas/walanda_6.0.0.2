<form method="post" action="{{ path('app_details_caisses_delete', {'id': details_caiss.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ details_caiss.id) }}">
    <button class="btn">Delete</button>
</form>
