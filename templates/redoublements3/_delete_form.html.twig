<form method="post" action="{{ path('app_redoublements3_delete', {'id': redoublements3.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ redoublements3.id) }}">
    <button class="btn">Delete</button>
</form>
