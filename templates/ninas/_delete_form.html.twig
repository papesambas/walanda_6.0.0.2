<form method="post" action="{{ path('app_ninas_delete', {'id': nina.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nina.id) }}">
    <button class="btn">Delete</button>
</form>
