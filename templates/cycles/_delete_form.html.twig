<form method="post" action="{{ path('app_cycles_delete', {'id': cycle.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cycle.id) }}">
    <button class="btn">Delete</button>
</form>
