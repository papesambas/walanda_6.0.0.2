<form method="post" action="{{ path('app_noms_delete', {'id': nom.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nom.id) }}">
    <button class="btn">Delete</button>
</form>
