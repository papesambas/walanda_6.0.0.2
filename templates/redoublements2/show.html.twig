{% extends 'base.html.twig' %}

{% block title %}Redoublements2{% endblock %}

{% block body %}
    <h1>Redoublements2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ redoublements2.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_redoublements2_index') }}">back to list</a>

    <a href="{{ path('app_redoublements2_edit', {'id': redoublements2.id}) }}">edit</a>

    {{ include('redoublements2/_delete_form.html.twig') }}
{% endblock %}
