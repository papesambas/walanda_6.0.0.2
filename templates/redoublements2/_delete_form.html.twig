<form method="post" action="{{ path('app_redoublements2_delete', {'id': redoublements2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ redoublements2.id) }}">
    <button class="btn">Delete</button>
</form>
