<form method="post" action="{{ path('app_scolarites3_delete', {'id': scolarites3.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scolarites3.id) }}">
    <button class="btn">Delete</button>
</form>
