{% extends 'base.html.twig' %}

{% block title %}Scolarites3 index{% endblock %}

{% block body %}
    <h1>Scolarites3 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Scolarite</th>
                <th>CreatedAt</th>
                <th>UpdatedAt</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for scolarites3 in scolarites3s %}
            <tr>
                <td>{{ scolarites3.id }}</td>
                <td>{{ scolarites3.scolarite }}</td>
                <td>{{ scolarites3.createdAt ? scolarites3.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ scolarites3.updatedAt ? scolarites3.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_scolarites3_show', {'id': scolarites3.id}) }}">show</a>
                    <a href="{{ path('app_scolarites3_edit', {'id': scolarites3.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_scolarites3_new') }}">Create new</a>
{% endblock %}
