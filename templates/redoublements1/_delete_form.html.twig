<form method="post" action="{{ path('app_redoublements1_delete', {'id': redoublements1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ redoublements1.id) }}">
    <button class="btn">Delete</button>
</form>
